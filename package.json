{
  "name": "evento-app",
  "version": "1.0.0",
  "description": "App de evento con mapa Leaflet, Bootstrap, formulario y tests con Vitest + jsdom",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "date-fns": "^4.1.0",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@vitest/coverage-v8": "^3.2.4",
    "jsdom": "^24.0.0",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
